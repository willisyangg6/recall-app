/**
 * Personalization copy that is a product contract, not incidental wording.
 *
 * The allergen preference is household-aware (C5.2B): one shared list, worded
 * so someone shopping for a child, a partner or a parent knows it is theirs to
 * use. Nothing behind it changed — there are no names, no per-person profiles,
 * no extra preference fields and no account. The words are the whole feature,
 * which is exactly why they live here and are asserted by a test rather than
 * being retyped inside a component.
 */

export const ALLERGEN_SECTION_LABEL = 'Allergens to watch';

export const ALLERGEN_SECTION_HELPER =
  'Select any allergens relevant to you or anyone you shop or cook for.';
